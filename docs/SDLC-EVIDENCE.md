# SDLC Evidence Map

**Deep phases:** 1. Discovery & Planning · 5. Verification & Testing
**Baseline phases:** 3. Architecture & Design · 4. Implementation · 6. Release & Deployment
**Intentionally light:** 7. Operations & Maintenance, 8. Retirement & Handover — because
this is a single-operator, self-hostable flagship where the deep evidence
budget is deliberately spent on proving the two claims the project exists to
demonstrate (grounded answers, tested security controls), not on operational
maturity. Reasons will be finalised once the deep-phase work lands.

| Phase | Depth | Evidence | Location |
|---|---|---|---|
| 1. Discovery & Planning | **Deep** | **Produced, Session 1 (2026-08-27):** validated problem statement, failure-cost analysis, and target-user reasoning (`00-project-brief.md`); explicit options-considered "why RAG, not fine-tuning or search alone" comparison (`00b-rag-vs-alternatives.md`); a real, executed feasibility spike against a real corpus — hybrid retrieval technically proven end-to-end, plus two honestly-reported findings (naive AND-semantics keyword search scored 0% recall@3; retrieval-similarity-only refusal is unsafe — a topically-adjacent-but-wrong query scored inside the correct-answer similarity range) — see `docs/spikes/session1-hybrid-retrieval/RESULTS.md`; 5 concrete success metrics; full non-goals table with reconsideration conditions | `docs/project-memory/00-project-brief.md`, `docs/project-memory/00b-rag-vs-alternatives.md`, `docs/project-memory/01-scope-and-non-goals.md`, `docs/spikes/session1-hybrid-retrieval/` |
| 2. Requirements Analysis | Baseline | **Produced, Session 2 (2026-08-27):** user stories with acceptance criteria for ingestion, querying, refusal, and audit flows; functional requirements traceable to the MVP boundary; non-functional requirements with real targets where the spike provides evidence and explicit "not set — no fabricated number" placeholders where it doesn't (retrieval quality at scale, refusal recall, citation accuracy, per-query cost); a full data-classification pass | `docs/project-memory/02-requirements.md` |
| 3. Architecture & Design | Baseline | **Produced, Session 2 (2026-08-27):** system context and container diagrams (Mermaid); the refusal-mechanism decision as this repo's first ADR — post-generation groundedness/entailment verification, chosen over reranking-only and pre-generation LLM-as-judge gating, directly against Session 1 spike Finding 2's measured numbers (`docs/adr/ADR-0001-groundedness-refusal-check.md`); the ingestion pipeline and hybrid-retrieval architecture (OR-semantics keyword + vector + RRF, fixed by the spike); LLM provider choice (Anthropic Claude, model-tier asymmetry for cost control) with the missing-API-credentials open item stated explicitly; a full data model (ERD) and API contract. **Extended, Session 3 (2026-08-27, security/threat modelling):** STRIDE threat model across 6 trust boundaries, with indirect prompt injection via ingested documents treated as the dominant risk and split into two named, distinctly-mitigated threats — generator hijack (T-01) and, as a first-class finding in its own right, verifier hijack (T-02), since ADR-0001's independent verification step is the last check in the pipeline; an embedding-inversion/information-leakage review confirming no API surface currently exposes raw embeddings; resolution of Session 2's open audit-trail tamper-evidence question (ADR-0002 — DB-level append-only enforcement, reasoned fresh against `privacy-forge`'s ADR-0003 rather than reused by default); a dedicated verification-hardening ADR (ADR-0003 — structured output, sandwiched delimiting, fail-closed-on-ambiguity, against passage-embedded prompt injection targeting the verifier specifically); cost-abuse/denial-of-wallet controls; accepted risks with revisit triggers | `docs/project-memory/03-architecture.md`, `docs/project-memory/04-data-model.md`, `docs/project-memory/05-api-contracts.md`, `docs/project-memory/06-security-threat-model.md`, `docs/adr/ADR-0001-groundedness-refusal-check.md`, `docs/adr/ADR-0002-audit-trail-tamper-evidence.md`, `docs/adr/ADR-0003-verification-injection-hardening.md` |
| 4. Implementation | Baseline | **Produced, Session 4 (2026-08-27):** the full ingest → retrieve → generate → verify → decide pipeline as real, tested application code — heading-aware chunking + `BAAI/bge-small-en-v1.5` embeddings (`ingestion/`), hybrid retrieval (OR-semantics keyword + vector + RRF, `retrieval/`), the two-call generate/verify pipeline (`llm/`, `pipeline/query_pipeline.py`) implementing ADR-0001's contract and ADR-0003's exact verification hardening schema, ADR-0002's two-role database permission split (Alembic migration `0002`), and the FR-005 `plainto_tsquery` code-level guard (`tests/test_retrieval_guard.py`). **Credential status, stated plainly: no Anthropic API key exists in this environment** — `llm/factory.py` selects the labeled, clearly-separated `StubLLMClient` (`llm/stub_client.py`) instead of `AnthropicLLMClient` (`llm/anthropic_client.py`), matching bookslot's `PaymentIntentGateway`/`FakePaymentIntentGateway` seam so a real key is a config change, not a rewrite. This is flagged as an **urgent, cheap-to-clear blocker** for Session 5, not a permanent workaround — see `docs/project-memory/12-session-handoff.md`'s Credential status section for the full reasoning. 33 tests pass against real Postgres+pgvector (no mocked DB), including a real ADR-0002 grant-assertion test, an NFR-001 recall@3 regression test against the Session 1 spike corpus, structural tests proving the generator and verifier carry genuinely different injection-hardening implementations (ADR-0003), and the ADR-0001-mandated proof test (below). Full backend CI (ruff, mypy, bandit, pip-audit, pytest against a real Postgres service) passes; `docker compose up` boots the real service end-to-end (verified via a live smoke-test query). | `backend/src/lexicon/`, `backend/tests/`, `backend/alembic/`, `.github/workflows/ci.yml` |
| **4a. The Session 1 adversarial-case proof test** | — | ADR-0001's Consequences section requires, as the harness's first test case, reproducing Session 1's actual 0.701-scoring OAuth2/JWT case through the real pipeline and confirming the verifier flags it as NOT grounded. `tests/test_proof_session1_oauth2_case.py` does this — ingests the real 8-document spike corpus, retrieves for the real query, and runs generation + verification. **Result, stated with the same honesty as bookslot's Claim-A/Claim-B distinction: this passed only against the stub tier (`llm.tier == "stub"`), which is wiring evidence, not entailment evidence** — `StubLLMClient.verify()` is a crude keyword-overlap heuristic, not real reasoning about whether a passage entails a claim (see that class's own module docstring). The test asserts real evidence (`llm.tier == "real"`, requiring `answered is False`) only once a real `ANTHROPIC_API_KEY` is configured; today it takes the wiring-only branch and asserts the pipeline reaches a well-formed terminal decision. This is explicitly **not** Session 5's full evaluation harness — it is the one sanity check ADR-0001 requires to exist the moment the verifier is first built. | `backend/tests/test_proof_session1_oauth2_case.py` |
| 5. Verification & Testing | **Deep** | Not yet produced — planned: committed golden-dataset-driven, CI-gated retrieval/answer evaluation harness; adversarial prompt-injection test suite treated as a real security control | `docs/project-memory/07-testing-strategy.md` (future: `eval/`, `docs/security/`) |
| 6. Release & Deployment | Baseline | CI pipeline (lint, type-check, security scan, tests) and `docker-compose.yml` proven to boot | `.github/workflows/ci.yml`, `docker-compose.yml` |
| 7. Operations & Maintenance | Light | Not yet produced | `docs/project-memory/08-deployment-and-operations.md` |
| 8. Retirement & Handover | Light | Not yet produced | `docs/project-memory/14-maintenance-and-retirement.md` |

## Why some phases are light

Operations and Retirement/Handover are intentionally light for this
repository: the deep-evidence budget (Rule D2, exactly two phases) is
committed to Discovery and Verification/Testing, because those two phases
carry the evidence for this project's actual claims — grounded, refusal-safe
answers, and prompt-injection defences that are tested rather than assumed.
Full reasoning to be recorded once Session 1 and the Verification/Testing
deep-phase work land.
